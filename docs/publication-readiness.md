# Source-alpha publication readiness

Audit date: **2026-09-14**. Repository: **brennansharpe/bjc85-modern**.

## Current status — 2026-09-15 (supersedes the dated preparation record)

The exact repository is PUBLIC SOURCE ALPHA; anonymous repository metadata and
advertised refs confirm public `main`. The authenticated owner connector confirms
admin access. No visibility, licensing or binary-release change is part of this
repair. The historical **STILL PRIVATE** outcome below describes preparation
before the later public transition, and is not the current state.

The supplied review retrieved an original printer-identity capture through
**authenticated access**. Anonymous retrieval of that old object was not verified
by that review. This repair separately rechecked the privately mapped original
capture: both authenticated access and an anonymous HTTPS request returned its
non-synthetic printer identifier. Sensitive bodies and links remain private. Normal cleaned refs and a fresh clone do not establish server-side
erasure. A printer serial is low-severity identifying metadata, not an account
credential. Retention and an owner/support decision remain unresolved: no owner
acceptance, sent support request or support resolution is claimed. Exact old
object mappings and identifying values remain outside this repository.

The reviewed story commit also introduced a personal author email. Local noreply
configuration alone does not repair historical metadata or account web-edit
settings. The metadata-only correction has now been pushed using an exact
expected-SHA lease and verified on GitHub; the owner's web-commit email privacy
setting was also enabled and verified. No server-side erasure is claimed.
See [the remediation record](review-remediation-2026-09-15.md) for current results
and external blockers. Earlier successful scans below are historical evidence,
not a claim that the reviewed failing workflow or this repair passed hosted CI.

## Historical outcome — preparation on 2026-09-14

**STILL PRIVATE.** Source preparation and independent remote verification are
recorded below. An original printer-identity capture remains
viewable through an old GitHub commit after the earlier history rewrite.
The preparation pass treated retained views as a visibility gate. The current
public status above supersedes that gate; support assistance is conditional. The private recovery record
contains the exact original object mappings and affected URLs; those links and
values are deliberately absent here. A fresh clone alone cannot establish
server-side erasure. No actual credential was identified by the local scans.

The earlier exported review omitted history and assets and could not access
GitHub. It was not used as publication clearance. This audit used the full
current checkout and the authenticated repository settings.

## Protected starting point and history

The starting checkout had no staged, unstaged or relevant untracked source
changes. A verified, owner-only local recovery directory outside this repository
preserves its Git object database/refs, working files, prior original evidence,
patch/status inventory, hashes and original-to-clean mappings. The private
backup is not a branch, tag, GitHub asset or dependency of the source build.
Original research and live runtime storage were not modified.

The authenticated owner and admin settings confirmed the exact repository,
private visibility and default branch `main`. The initial remote advertised only
`main`, with no tags or PR refs. Both local historical milestone tags were also
audited. Local editor snapshot refs were inventoried and preserved privately;
they are not publication refs. No LFS pointers, submodules, gitlinks, vendored
dependency trees or binary release packages were present in relevant history.

The prior cleanup replaced unnecessary printer identity, home paths, account
labels and non-synthetic UUIDs, removed the machine-labelled Accessibility
Inspector screenshot, and stripped metadata from 27 retained JPEG captures.
Those captures and text files are privacy derivatives, not untouched historical
hardware evidence. Six binary status/calibration fixtures remain byte-identical;
their exact hashes and purpose are in `scripts/privacy-reviewed.json`.
Serial/head/temperature validation and per-device calibration were not changed.

This pass narrowly repairs the earlier metadata rewrite's Codex attribution,
restoring its original nonpersonal identity. Owner commits use the verified
GitHub noreply identity through repository-local configuration. Names and useful
commit history are preserved. Only the explicitly reviewed `main` ref is to be
updated, with an exact expected-remote-SHA lease; no mirror push or backup refs.

## Inspected content and justified exceptions

The initial normal-ref history contained four commits and 394 unique blobs.
Tracked files, every branch/tag version, commit/tag messages and identities were
checked. Specific prior serial/path findings in bring-up, partial-print/USB
baselines, native tests and build/test logs were rechecked. The hardening audit
JSON remains historical, with a current superseding note.

All 34 unique historical raster images were visually reviewed and fully decoded;
metadata/container checks found no retained metadata or trailing payloads.
Seven unique SVGs were inspected as XML/source; six correspond to the reviewed
gallery renders and the older wireframe was rendered for review. No embedded
images, scripts, foreign objects or external resource references were present.
See [the image hash inventory](verification/publication-images.json). Mockups
remain explicitly labelled; fixture captures show synthetic content. The removed
Inspector image is absent from publication refs, and its original stays private.

Useful protocol constants, USB vendor/product IDs, upstream hashes and resource
IDs remain. The resource index contains names, IDs, sizes, attributes and hashes,
not proprietary payloads. Synthetic UUIDs have an explicit reserved prefix;
redacted/test serials, example email domains, GitHub noreply identities and
loopback/documentation addresses are explained exceptions. Exact binary fixture
exceptions do not disable text or known-private-value matching. No whole folder
was exempted to obtain a pass.

## Prevention and independent scanning

- Python privacy checker: ASCII/UTF-8, UTF-16LE/BE and alignments; privately
  configured known-value variants; bounded PNG/JPEG and archive inspection;
  explicit malformed/unsupported/truncated/encrypted/limit failures.
- Thirteen synthetic regression tests cover Markdown serials, tokens after NUL,
  UTF-16 private keys, metadata, archive bounds, output redaction, local config,
  deleted historical findings, actual new branch/tag pushes and hook conflicts.
- The committed pre-push hook checks outgoing objects and requires an external,
  permission-restricted private configuration. Installation preserves unrelated
  hooks. Generic CI explicitly lacks the owner's private-value list.
- Gitleaks **8.30.1**, downloaded from its official release and checked against
  GitHub's release-asset SHA-256, passed a synthetic token positive control.
  Redacted scans of the tracked candidate, normal-ref history, all 398 original
  candidate blobs/commit metadata exported locally, and the privately retained
  pre-cleanup history returned no credential findings. No credential was tested
  against a service and no private material was sent to a scanning service.
- Git-filter-repo **2.47.0** performed the narrow identity correction in a separate
  checkout after the verified backup; current upstream usage documentation was
  checked. GitHub CLI **2.100.0** and current visibility/removal documentation
  were checked. CLI authentication is unavailable; authenticated browser settings
  and the authorized temporary repository-only Git transport establish access.

Darwin Gitleaks archive SHA-256:
`b40ab0ae55c505963e365f271a8d3846efbc170aa17f2607f13df610a9aeb6a5`.
Raw findings, old-object links, private values and tool logs stay outside Git.
Pattern scanners and visual inspection reduce risk; neither is exhaustive proof.

## GitHub-only surfaces

Authenticated inspection found no Releases/assets, open or closed issues, open
or closed pull requests, Actions caches or deployment environments. The four current-history commit
views show zero commit comments; no self-hosted runners are configured. Actions
initially showed workflow setup and there were no workflow files in history.
Wiki and Pages are unavailable for this private repository's current plan;
Discussions are disabled. Disabled/unavailable features are not proof that
GitHub has erased all retained server-side data. The old commit capture is a
verified unresolved exposure finding. No unrelated GitHub content was deleted.

The new privacy CI uses a pinned checkout action, checksum-pinned Gitleaks,
read-only contents permission and GitHub-hosted Linux runners. It has no secrets,
private-value list, signing credentials, privileged PR trigger, artifact upload
or access to the owner's Mac/printer. No paid feature or subscription is enabled.

## Licensing and provenance

No project-wide open-source licence has been granted. This is not a new licence
or an all-rights-reserved claim over third-party components. Actual pinned
Gutenprint 5.3.5, PAPPL 1.4.12, libusb 1.0.30 and OpenSSL 3.6.4 notice files were
read. Their sources are local build inputs, not vendored public content.
See [THIRD_PARTY](../THIRD_PARTY.md) for the source-only boundary, notices and
recommended future GPLv3-compatible binary route requiring an owner decision.
Original Canon installers/manuals, proprietary artwork/profiles/resources and
full decompilations remain excluded. Static-analysis provenance is retained;
no Canon endorsement or independently staffed clean-room process is claimed.

## Offline verification and alpha limitations

A clean source-only candidate on Apple silicon with the installed Xcode toolchain
built the C targets and passed **9/9 C tests**, **7 Swift model/UI suites** and
**4 Swift hardening suites** using `scripts/test-source-alpha.sh`. No private
capture or installed service was required. The optional Gutenprint capability
test was not configured in this source-only checkout; private full-scan replays,
isolated service-bundle tests, packaging and physical acceptance were not run.
Privacy regressions passed separately after the final checker changes.

No physical operations, live document/recovery/calibration storage, system
services, signing credentials or macOS protections were changed. No binary was
uploaded. Full Copy-cycle hardware acceptance, supported-OS runtime validation,
VoiceOver qualification, signing/notarization and the known black-ink fault
remain open. Unavailable legacy features remain disclosed in the README and
acceptance ledgers; none is concealed by offline test success.

## Final remote verification

The prepared source was pushed while private using the exact-SHA lease and
cloned independently from GitHub. Its commit/tree and all 41 raster/vector hashes
matched the reviewed candidate; the fresh clone passed the populated local
privacy check and Gitleaks history scan. Only `main` was advertised; no backup,
stash, original-history or local milestone tags were uploaded.

[CI run 1](https://github.com/brennansharpe/bjc85-modern/actions/runs/34911061230)
passed all thirteen privacy regressions, the generic tree/history checker and
Gitleaks tree/history scans. Logs show a masked checkout token, generic hosted
runner paths and no private matching configuration; no artifacts were uploaded.
Its Node 20 action warning was addressed by pinning checkout v6.0.3's actual
commit (Node 24). That small workflow/documentation follow-up is checked again
on the final remote ref; exact commit and recovery information are retained in
the private completion record.

The everyday checkout is reconciled to the cleaned lineage, with the local
privacy hook and private matching configuration installed. Original-history
recovery copies are backup-only: never push or merge them into development.
No global Git identity was changed. The temporary repository-only deploy key
is revoked after the upload/verification.

Visibility remains private. Request GitHub Support's retained-object cleanup
using the privately prepared request, then repeat the final remote/ref/asset and
new workflow-log checks before changing visibility. No public transition or
anonymous public-access verification is claimed. The repository must not be
recreated or replaced.

References: [GitHub sensitive-data removal](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository),
[GitHub visibility command](https://cli.github.com/manual/gh_repo_edit),
[Git-filter-repo documentation](https://github.com/newren/git-filter-repo/blob/main/Documentation/git-filter-repo.txt),
[Gitleaks releases](https://github.com/gitleaks/gitleaks/releases).
